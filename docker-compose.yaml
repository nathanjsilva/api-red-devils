version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api_red_devils_app
    working_dir: /var/www
    volumes:
      - ./:/var/www
    networks:
      - api_red_devils_network
    depends_on:
      - mysql

  nginx:
    image: nginx:latest
    container_name: api_red_devils_nginx
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - api_red_devils_network
    depends_on:
      - app

  mysql:
    image: mysql:8.0
    container_name: api_red_devils_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: api_red_devils
      MYSQL_USER: laravel
      MYSQL_PASSWORD: 1234
    ports:
      - "3306:3306"
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - api_red_devils_network

volumes:
  dbdata:

networks:
  api_red_devils_network:
    driver: bridge
